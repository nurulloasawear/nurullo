Один или несколько параметров команды отсутствуют либо недопустимы.

Использование: connect [name=]<строка> [[ssid=]<строка>] [[interface=]<строка>] 

Параметры:

    Тег             Значение
    ssid          - Имя (SSID) беспроводной сети.
    name          - Имя профиля, используемого для подключения.
    interface     - Имя интерфейса, используемого для подключения.

Примечания

    Подключение к беспроводной сети с заданным именем ssid, с использованием
    указанного профиля. Подключение будет выполнено с использованием указанного
    интерфейса; если в системе доступен только один интерфейс, то параметр
    interface можно не указывать.

    Имя профиля является обязательным параметром, а ssid - необязательным.
    Если в профиле существует только один SSID, тогда этот SSID используется
    для подключения. Если в профиле существует несколько SSID, тогда параметр
    "SSID" необходимо задать.

    Параметр interface является обязательным при наличии в системе двух и более
    интерфейсов. Имя интерфейса не должно содержать подстановочных символов.

    Если указанный интерфейс уже подключен к беспроводной сети,
    эта команда сначала выполнит отключение от подключенной в данный момент
    сети, а затем попытается подключиться к новой сети. Но если это одна и та
    же сеть, то никаких действий не предпринимается.

Примеры:

    connect name=профиль1 ssid=SSID1
    connect name=профиль2 ssid=SSID2 interface="Беспроводное сетевое соединение"

